# Contributing

The pattern is deliberately small. A proposed change needs one thing: a contract that followed the pattern as written and still shipped the wrong thing, stated as a scenario (the contract, what shipped, what the gate said, what was actually wrong). Wording improvements that do not change what the pattern catches are welcome as plain PRs.

Do not add tooling here beyond the schema. This repository is the pattern; enforcement is whatever your organization builds around it.
